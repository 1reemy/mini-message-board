extends layout

block content
  h1= title
  p Welcome to #{title}
  ul 
    each message in messages.length ? messages : ['No messages']
      li= messages.message.user && messages.message.text
  a(href="/new") Enter New Message